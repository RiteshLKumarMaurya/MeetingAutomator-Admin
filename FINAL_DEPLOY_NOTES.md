# Meeting Automator Admin — Final Release

## Included fixes
- Added **Contact Requests** to the protected admin navigation so website enquiries are directly accessible.
- Improved Site Modules with loading, retry, refresh, empty-state and live-count handling.
- Added a production environment example using `https://api.meetingautomator.com`.
- Added the missing `npm run check` script used by the production deploy script.
- Admin remains `noindex` so the protected control panel is not intended for search indexing.
